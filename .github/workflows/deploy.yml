name: Deploy presentations to GitHub Pages

on:
  push:
    branches: [main]
    paths-ignore: ['**/README.md']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2

      - name: Build each presentation
        run: |
          set -e
          mkdir -p site
          cp index.html site/

          for dir in */; do
            dir="${dir%/}"
            [ -f "$dir/slides.md" ] || continue

            echo "Building $dir"
            # --download exports slidev-exported.pdf into dist and adds a download button
            (cd "$dir" && bun install --frozen-lockfile \
              && bunx playwright install --with-deps chromium \
              && bun run build -- --base "/$dir/" --download)

            mkdir -p "site/$dir"
            cp -r "$dir/dist/." "site/$dir/"
          done

      - uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
